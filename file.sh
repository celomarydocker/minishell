#!bin/bash
echo hello > a > b > c | grep sh | ls | sort | sort -r | sort | sort -r | cat < m < n < a ; cd; export key= str key1 = str key2=hello; export | grep PATH; 
exit | echo hello | exit; pwd > cur | pwd | cat;
